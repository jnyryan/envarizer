{
  "name": "envarizer",
  "version": "0.0.5",
  "description": "takes a bash shell export command and turns it into a json object",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "snyk test && mocha --reporter progress --compilers coffee:coffee-script/register"
  },
  "bin": {
    "envarize": "bin/app.js"
  },
  "author": "John Ryan",
  "license": "ISC",
  "dependencies": {
    "byline": "^4.2.1",
    "coffee-script": "^1.10.0",
    "docopt": "^0.6.2"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.4.5",
    "snyk": "^1.14.3"
  }
}